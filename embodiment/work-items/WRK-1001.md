# WRK-1001 — Attach one visible body

Status: proposed; implementation not started.

## Objective

Attach one visible body. See DELIVERY.md for the user-visible contribution.

## Dependencies and requirements

Dependencies: WRK-1004 (following WRK-1000). Requirements: REQ-1001, REQ-1002, REQ-1003, REQ-1004, REQ-1008. Decisions: DEC-1001, DEC-1002, DEC-1004; renderer default DEC-1003 remains provisional.

## Starting evidence

- Source reconciled by WRK-1000; minimum persistent Core, capability dispatch and snapshot seams provided by WRK-1004.
- Reuse the original ModelPort/LM Studio adapter/configuration; body actions use the published supported request route.

## Scope

Actual Core CapabilityPort/InspectState paths found in WRK-1000; new body adapter/viewer and model manifest. Exact executable paths and commands are recorded from the real checkout in WRK-1000. New source directories should follow that checkout; do not guess or recreate a host.

## Steps

1. Load one compatible model in a local viewer; report its supported actions.
2. Connect through existing Core and BodyPort; persist meaningful request/receipt events.
3. Request wave, inspect its result and exercise reconnect and unsupported action.

## Acceptance

- VER-1001 and VER-1002 demonstrations succeed.
- Core remains usable when viewer closes; no fresh companion is created.

## Preserved contracts and exclusions

Preserve Core single-writer and action evidence, companion identity across bodies, and AUTONOMY-AMENDMENT.md. No automatic identity shaping, second memory host, or unrelated roadmap work. No implementation is authorized solely by this document.

## Evidence and recovery

Record code revision, actual launcher/check commands, a short demonstration result and relevant receipt under the project's existing evidence convention. Use the existing backup path before additive migrations; retain prior assets for rollback. If the actual core is inaccessible, record the dependency and request its location without building a substitute.

## Completion

Not started. No runtime evidence yet.
